<Window x:Class="ConfigStringManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Config String Manager" Height="760" Width="1280"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#EFEFEF"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#EFEFFF"/>
        <SolidColorBrush x:Key="PanelBorderBrush" Color="#D2D2D2"/>
        <SolidColorBrush x:Key="TextBrush" Color="#242A69"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#101985"/>
        <SolidColorBrush x:Key="AccentBrush" Color="Transparent"/>


        <Style TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="Padding" Value="6,4" />
            <Setter Property="Margin" Value="2" />
        </Style>

        <!-- TreeView dark bg & item style -->
        <Style TargetType="TreeView">
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="4,3"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="FontWeight" Value="Bold" />

                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E3E3E3"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="white"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
            <Setter Property="Padding" Value="6"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="#081014"/>
            <Setter Property="Padding" Value="4"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="white"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackgroundBrush}" Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" />
            <ColumnDefinition Width="3.5*" />
            <ColumnDefinition Width="2.2*" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: Files Tree -->
        <Border Grid.Column="0" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="10" Margin="0,0,121,0">
            <DockPanel>
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                    <Button Content="Add File" Click="BtnAddFile_Click" />
                    <Button Content="Remove File" Click="BtnRemoveFile_Click" Margin="6,0,0,0"/>
                </StackPanel>
                <TreeView x:Name="FilesTree" Width="280"/>
            </DockPanel>
        </Border>

        <!-- CENTER: Connection editor -->
        <Border Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="317,0,124,0" Grid.ColumnSpan="2">
            <StackPanel>
                <TextBlock Text="Edit Selected" Foreground="{StaticResource MutedBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                <StackPanel x:Name="AliasPanel" Visibility="Collapsed">
                    <Separator Margin="0,4,0,8"/>
                    <!-- Alias edit row -->
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,8">
                        <StackPanel Width="70">
                            <TextBlock Text="Alias" Foreground="{StaticResource MutedBrush}"/>
                        </StackPanel>
                        <TextBox x:Name="AliasBox" MinWidth="300" TextChanged="AliasBox_TextChanged" Width="140" />
                        <Button Content="Save Alias" Click="BtnSaveAlias_Click" Margin="0"/>
                    </StackPanel>

                    <!-- File path -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <StackPanel Width="70">
                            <TextBlock Text="File" Foreground="{StaticResource MutedBrush}" />
                        </StackPanel>
                        <TextBlock x:Name="FilePathText" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap" MinWidth="300" MaxWidth="300"/>
                        <Button x:Name="btnCopyText" Click="BtnCopyFilePath_Click" Content="Copy" Background="{StaticResource PanelBrush}" Foreground="Black" Padding="8,6" Margin="0"/>
                    </StackPanel>

                    <StackPanel x:Name="CopiedTextStatus" Orientation="Horizontal" Margin="0,0,0,12" Visibility="Collapsed">
                        <TextBlock Text="Path copied!" Foreground="DarkRed" MinWidth="300" MaxWidth="300" FontWeight="Bold"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <StackPanel Width="70">
                            <TextBlock Text="Server" Foreground="{StaticResource MutedBrush}"/>
                        </StackPanel>
                        <ComboBox x:Name="FileServersCombo" MinWidth="300" IsEditable="False"/>
                        <Button x:Name="btnUpdateFileServers" Click="BtnUpdateServersOnFile_Click" Content="Update" Background="{StaticResource PanelBrush}" Foreground="Black" Padding="8,6" Margin="0"/>

                    </StackPanel>
                    <StackPanel>
                        <TextBlock x:Name="StatusTextFileServers" Foreground="DarkRed" FontWeight="Bold" Margin="10,6,0,0"/>
                    </StackPanel>

                </StackPanel>

                <Separator Margin="0,4,0,8"/>

                <StackPanel x:Name="ConnStringsPanel" Visibility="Collapsed">
                    <!-- Connection entry -->
                    <TextBlock Text="Connection" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <StackPanel Width="70">
                            <TextBlock Text="Name" Foreground="{StaticResource MutedBrush}"/>
                        </StackPanel>
                        <TextBlock x:Name="ConnNameText" Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <StackPanel Width="70">
                            <TextBlock Text="Server" Foreground="{StaticResource MutedBrush}"/>
                        </StackPanel>
                        <ComboBox x:Name="ServerCombo" MinWidth="300" IsEditable="False" SelectionChanged="ServerCombo_SelectionChanged"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <StackPanel Width="70">
                            <TextBlock Text="Database" Foreground="{StaticResource MutedBrush}"/>
                        </StackPanel>
                        <ComboBox x:Name="DatabaseCombo" MinWidth="300" IsEditable="True"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal">
                        <Button x:Name="btnSaveChanges"  Content="Save Changes" Click="BtnSaveConn_Click" Background="{StaticResource PanelBrush}" Foreground="Black" Padding="8,6"/>
                        <TextBlock x:Name="StatusText" Foreground="{StaticResource MutedBrush}" Margin="10,6,0,0"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- RIGHT: Server manager -->
        <Border Grid.Column="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource PanelBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Grid.ColumnSpan="2" Margin="386,0,0,0">
            <StackPanel>
                <TextBlock Text="Servers" Foreground="{StaticResource MutedBrush}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <StackPanel>
                        <TextBlock Text="Friendly name" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                        <TextBox x:Name="ServerNameBox" Width="160" Margin="0,2,6,0" TextChanged="ServerNameBox_TextChanged" />
                    </StackPanel>
                    <StackPanel>
                        <TextBlock Text="Address" Foreground="{StaticResource MutedBrush}" FontSize="11"/>
                        <TextBox x:Name="ServerAddressBox" Width="262" Margin="0,2,6,0" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
                    <CheckBox x:Name="UseSqlAuthCheck" Content="Use SQL Auth" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <TextBox x:Name="SqlUserBox" Width="162" Margin="0,0,6,0"/>
                    <PasswordBox x:Name="SqlPassBox" Width="172" BorderBrush="{StaticResource PanelBorderBrush}" VerticalAlignment="Center" Padding="0,8"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="Add Server" Click="BtnAddServer_Click" Margin="0,0,6,0" />
                    <Button Content="Test Connection" Click="BtnTestConnection_Click" Margin="0,0,6,0"/>
                    <Button Content="Remove Selected" Click="BtnRemoveServer_Click" Margin="0,0,6,0"/>
                </StackPanel>

                <ListBox x:Name="ServersListBox" Height="300" DisplayMemberPath="Display" />
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">

                </StackPanel>
                <TextBlock x:Name="ServerTestResult" Foreground="red" Margin="0,8,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
